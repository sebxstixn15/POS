<Window x:Class="Waldwunderverwaltung.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Waldwunderverwaltung – Österreichische Bundesforste"
        MinWidth="700" MinHeight="480"
        Width="1000" Height="640"
        Background="#F0F4F0">

    <Window.Resources>
        <!-- Styled menu -->
        <Style TargetType="MenuItem">
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <!-- Section header -->
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2D4A3E"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Search TextBox -->
        <Style x:Key="SearchBox" TargetType="TextBox">
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="BorderBrush" Value="#9DBF9E"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="30"/>
        </Style>

        <!-- Primary button -->
        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Background" Value="#1B4332"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="14,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#2D6A4F"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Background" Value="#95A5A6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- RadioButton style -->
        <Style TargetType="RadioButton">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,12,0"/>
            <Setter Property="Foreground" Value="#1B4332"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <DockPanel>
        <!-- ── MENU BAR ── -->
        <Menu DockPanel.Dock="Top" Background="#1B4332" Foreground="White"
              FontSize="13" FontWeight="SemiBold" Padding="6,4">
            <Menu.Resources>
                <Style TargetType="MenuItem">
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Background" Value="#1B4332"/>
                    <Setter Property="FontSize" Value="13"/>
                </Style>
            </Menu.Resources>
            <MenuItem Header="🌲 Waldwunderverwaltung" IsEnabled="False"
                      Foreground="#74C69D" FontWeight="Bold"/>
            <MenuItem Header="Datei">
                <MenuItem Header="📋 Neues Waldwunder" x:Name="mnuNeuesWaldwunder"
                          Click="MnuNeuesWaldwunder_Click"/>
                <Separator/>
                <MenuItem Header="Beenden" Click="MnuBeenden_Click"/>
            </MenuItem>
        </Menu>

        <!-- ── TITLE BAR ── -->
        <Border DockPanel.Dock="Top" Background="#2D6A4F" Padding="14,8">
            <TextBlock Text="🌲 Waldwunderverwaltung" FontSize="16" FontWeight="Bold"
                       Foreground="White"/>
        </Border>

        <!-- ── SEARCH PANEL ── -->
        <Border DockPanel.Dock="Top" Background="White"
                BorderBrush="#C8D8C8" BorderThickness="0,0,0,1" Padding="14,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Radio buttons -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Style="{StaticResource SectionHeader}" Text="Suche:" Margin="0,0,12,0"/>
                    <RadioButton x:Name="rbStichwort" Content="Stichwort" IsChecked="True"
                                 Checked="SearchMode_Changed"/>
                    <RadioButton x:Name="rbArt" Content="Art"
                                 Checked="SearchMode_Changed"/>
                    <RadioButton x:Name="rbOrt" Content="Ort"
                                 Checked="SearchMode_Changed"/>
                </StackPanel>

                <!-- Search inputs (switch based on mode) -->
                <Grid Grid.Column="2">
                    <!-- Stichwort / Art input -->
                    <TextBox x:Name="txtSuchbegriff" Style="{StaticResource SearchBox}"
                             x:FieldModifier="private"
                             ToolTip="Suchbegriff eingeben"
                             KeyDown="TxtSuchbegriff_KeyDown"/>

                    <!-- Ort inputs (Lat / Lon) -->
                    <Grid x:Name="ortPanel" Visibility="Collapsed">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="120"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Latitude:" Style="{StaticResource SectionHeader}"/>
                        <TextBox Grid.Column="2" x:Name="txtLat" Style="{StaticResource SearchBox}"
                                 ToolTip="z.B. 48.131836"/>
                        <TextBlock Grid.Column="4" Text="Longitude:" Style="{StaticResource SectionHeader}"/>
                        <TextBox Grid.Column="6" x:Name="txtLon" Style="{StaticResource SearchBox}"
                                 ToolTip="z.B. 16.687295"/>
                    </Grid>
                </Grid>

                <Button Grid.Column="4" x:Name="btnSuchen" Content="🔍 Suchen"
                        Style="{StaticResource PrimaryBtn}"
                        Click="BtnSuchen_Click"/>
            </Grid>
        </Border>

        <!-- ── BOTTOM ACTION BAR ── -->
        <Border DockPanel.Dock="Bottom" Background="White"
                BorderBrush="#C8D8C8" BorderThickness="0,1,0,0" Padding="14,10">
            <DockPanel>
                <TextBlock x:Name="txtStatus" VerticalAlignment="Center"
                           Foreground="#555" FontStyle="Italic" FontSize="12"/>
                <Button x:Name="btnAnzeigen" DockPanel.Dock="Right"
                        Content="📄 Anzeigen" Style="{StaticResource PrimaryBtn}"
                        IsEnabled="False" Click="BtnAnzeigen_Click"/>
            </DockPanel>
        </Border>

        <!-- ── MAIN CONTENT ── -->
        <Grid Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" MinWidth="200"/>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="*" MinWidth="300"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Result List -->
            <Grid Grid.Column="0" Background="#F8FAF8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#E8F4E8" Padding="10,6"
                        BorderBrush="#C8D8C8" BorderThickness="0,0,0,1">
                    <TextBlock Text="Suchergebnisse" FontWeight="SemiBold"
                               Foreground="#1B4332" FontSize="13"/>
                </Border>

                <ListBox Grid.Row="1" x:Name="lstResults"
                         BorderThickness="0" Background="Transparent"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         SelectionChanged="LstResults_SelectionChanged">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="8,6"/>
                            <Setter Property="BorderBrush" Value="#E0EEE0"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#C8E6C9"/>
                                    <Setter Property="Foreground" Value="#1B4332"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#E8F5E9"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontWeight="Bold"
                                           FontSize="13" Foreground="#1B4332"
                                           TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding Description}" FontSize="11"
                                           Foreground="#555" TextWrapping="Wrap"
                                           MaxHeight="36" TextTrimming="CharacterEllipsis"/>
                                <TextBlock FontSize="11" Foreground="#888" Margin="0,2,0,0">
                                    <Run Text="{Binding Province}"/>
                                    <Run Text=" · "/>
                                    <Run Text="{Binding Type}"/>
                                </TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch"
                          Background="#C8D8C8"/>

            <!-- Right: Map -->
            <Grid Grid.Column="2" Background="#E8F5E9">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#E8F4E8" Padding="10,6"
                        BorderBrush="#C8D8C8" BorderThickness="0,0,0,1">
                    <TextBlock Text="Karte – Österreich" FontWeight="SemiBold"
                               Foreground="#1B4332" FontSize="13"/>
                </Border>

                <!-- Map Canvas -->
                <Canvas x:Name="mapCanvas" Grid.Row="1"
                        ClipToBounds="True"
                        SizeChanged="MapCanvas_SizeChanged">
                    <Canvas.Background>
                        <ImageBrush x:Name="mapImageBrush"
                                    ImageSource="Images/blobid0.png"
                                    Stretch="Fill"/>
                    </Canvas.Background>
                </Canvas>
            </Grid>
        </Grid>
    </DockPanel>
</Window>
