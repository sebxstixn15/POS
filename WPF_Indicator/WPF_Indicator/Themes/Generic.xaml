<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:WPF_Indicator">
    <Style TargetType="{x:Type local:Indicator}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:Indicator}">
                    <Grid>
                        <Image Source="/WPF_Indicator;component/resources/blobid1.png" 
                                Stretch="Uniform">
                        </Image>
                        <Image Source="/WPF_Indicator;component/resources/blobid2.png" 
                               Stretch="Uniform"
                               x:Name="needle"
                               RenderTransformOrigin="0.5,0.5">
                            <Image.RenderTransform>
                                <RotateTransform Angle="{Binding Path=(local:Indicator.Angle), RelativeSource={RelativeSource TemplatedParent}}" />
                            </Image.RenderTransform>
                        </Image>
                        
                        <StackPanel VerticalAlignment="Bottom" Margin="0,0,0,20">
                            <TextBlock Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Value, StringFormat={}{0:F1}}" 
                                       HorizontalAlignment="Center" FontSize="20" FontWeight="Bold"/>
                            <UniformGrid Columns="2">
                                <TextBlock Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Minimum}" HorizontalAlignment="Center" FontSize="10"/>
                                <TextBlock Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Maximum}" HorizontalAlignment="Center" FontSize="10"/>
                            </UniformGrid>
                        </StackPanel>
                    </Grid>

                </ControlTemplate>
                
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
