<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Countdown">
    <Style TargetType="{x:Type local:Countdown}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:Countdown}">
                    <Border Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <StackPanel>
                            <TextBlock Foreground="Red" FontSize="36" 
                               x:Name="PART_CURRENTDATETIME"></TextBlock>
                            <CheckBox x:Name="PART_CHECKBOXALARMSET" >Countdown Set</CheckBox>
                            <Button x:Name="PART_SETALARMBUTTON" >
                                <StackPanel>
                                    <TextBlock FontSize="24">Set Countdown Time</TextBlock>
                                    <TextBlock FontSize="24" 
                                       Text="{Binding CountdownTimerVisual, 
                                              RelativeSource={RelativeSource TemplatedParent}, 
                                              StringFormat='{}{0:mm:ss}'}">             
                                    </TextBlock>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
